class Validation {
  static bool isValidName(String name) => true;
  static bool isValidEmail(String email) => true;
  static bool isValidPassword(String password) => true;
  static bool isValidUsername(String username) => true;
}
